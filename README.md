# Novel Grain — React MVP

## Quick Start
```bash
npm install
npm run dev
```
Then open the local URL (e.g. http://localhost:5173).

Stack: React 18, Vite 5, Tailwind 3, Framer Motion, lucide-react.
